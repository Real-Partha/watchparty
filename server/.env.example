# Server environment example
# Bind host/port
HOST=0.0.0.0
PORT=8080

# CORS: comma-separated list; supports exact URLs and wildcards like https://*.vercel.app
ORIGIN=https://watchparty-frontend-dun.vercel.app

# Optional TLS (if running HTTPS yourself)
# SSL_CERT_FILE=
# SSL_KEY_FILE=
